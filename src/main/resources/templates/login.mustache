<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Home</title>
    <style>
    img {
        border-radius: 50%;
    }
    .card {
    background-color: #F2F2F2;
    border-radius: 10px;
    padding: 20px;
    margin: 15px;
}

    </style>
</head>
<body>
<div class="col-md-12">
    <div class="row">
        <div class="col-md-6">
            <!--userName 이 있으면 -->
            {{#userName}}
                {{#picturename}}
                    <img id="imgId" src="/spaces/{{spaceId}}/picture" width="100" height="100" alt="첨부이미지" /><br><br>
                {{/picturename}}
                {{^picturename}}
                    <img class="photo" width="90" height="90" src="{{ userPicture }}" /><br><br>
                {{/picturename}}
                Logged in as (name) : <span id="user">{{userName}}</span><br>
                Logged in as (email) : <span id="user">{{userEmail}}</span><br>
               <!-- Logged in as (picture) : <span id="user">userPicture}}</span>-->
                <a href="/logout" class="btn btn-primary" role="button">Logout</a><br>
                <a href="/spaces/{{spaceId}}" class="btn btn-primary">내 스페이스</a>
            {{/userName}}
            <!--userName 이 없으면 -->
            {{^userName}}
                <a href="/oauth2/authorization/google" class="btn btn-success active" role="button">Google Login</a>
            {{/userName}}
        </div>
    </div>
</div>
</body>
</html>

